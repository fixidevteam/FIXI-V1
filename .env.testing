APP_NAME=Laravel
APP_ENV=testing
APP_KEY=base64:eKSC9B1BPWyvwN0c3voUZSQxOGsQ41o0Dm8xw9MJvl4=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Use SQLite for faster, isolated testing
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# If you must use MySQL, ensure the database is separate from your development database
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=testing_database
# DB_USERNAME=root
# DB_PASSWORD=Youssefwac2003

# Use array or null drivers for caching, sessions, and queues
BROADCAST_DRIVER=log
CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
SESSION_LIFETIME=120

# Disable external services like Redis and Memcached
MEMCACHED_HOST=127.0.0.1
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Use the log mailer to avoid sending real emails during testing
MAIL_MAILER=log
# MAIL_HOST=smtp.hostinger.com
# MAIL_PORT=465
# MAIL_USERNAME=notification@fixi.ma
# MAIL_PASSWORD="ydrIql7H5="
# MAIL_ENCRYPTION=ssl
# MAIL_FROM_ADDRESS="notification@fixi.ma"
# MAIL_FROM_NAME="FixiPlus Notifications"

# AWS and Pusher configurations (disable or mock these services)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"